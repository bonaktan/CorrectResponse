import React from "react";
import ReactDOM from "react-dom/client";

import CorrectResponser from "./App/UI/main.jsx";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
    <React.StrictMode>
        <CorrectResponser />
    </React.StrictMode>,
);
